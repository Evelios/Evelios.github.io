# Github Pages Hosted Website

This project uses the [midnight](https://pages-themes.github.io/midnight/) theme for the site.

## Editing the website

In the project run `bundle exec jekyll serve` and navigate to `http://localhost:4000/` in the browser.
